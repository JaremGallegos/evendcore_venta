@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
    var roles = new List<(string rol, string[] modulos)>() {
        ("admin", new string[] {"Inicio", "Ventas", "Configuración", "Facturación"}),
        ("usuario", new string[] {"Inciio", "Ventas", "Configuracion"})
    };
}
<aside 
    x-show="isOpen" 
    x-transition:enter="transition ease-out duration-300"
    x-transition:enter-start="transform -translate-x-full"
    x-transition:enter-end="transform translate-x-0"
    x-transition:leave="transition ease-in duration-300"
    x-transition:leave-start="transform translate-x-0"
    x-transition:leave-end="transform -translate-x-full"
    class="fixed inset-y-0 left-0 w-64 bg-white shadow-xl z-50">
    
    <!-- Usuario -->
    <div class="flex items-center gap-3 p-3 border-b">
        <div class="w-8 h-8 bg-gray-300 rounded-full">
            <img src="" alt="Esta es una imagen del usuario" />
        </div>
        <select class="text-sm font-medium w-full bg-transparent border-0">
            <option value="admin">Admin</option>
            <option value="user">User</option>
        </select>
        <button @@click="isOpen = false" class="text-gray-500 hover:text-gray-700 focus:outline-none">
            <div class="flex w-1 h-1 rounded-full mb-6">
                <i class="fa-solid fa-x"></i>
            </div>
        </button>
    </div>

    <!-- Buscador -->
    <div class="p-3 my-0">
        <div class="relative">
            <input type="text" placeholder="Buscar" class="w-full pl-8 py-2 border rounded bg-gray-50"/>
            <div class="absolute left-2 top-1/3 transform -translate-y-1/2 h-4 w-4 text-gray-500 mt-1">
                <i class="fa-solid fa-magnifying-glass"></i>
            </div>
        </div>
    </div>

    <!-- Navegación -->
    <nav class="flex-1 space-y-1 p-3">
        <a href="@Url.Action("Index", "Dashboard")" class="flex items-center gap-2 p-2 rounded haver:bg-gray-100">
            <svg class="h-4 w-4"><!-- Icono --></svg>
            Dashboard
            <span class="ml-auto text-xs text-gray-500">2</span>
        </a>
        <a href="@Url.Action("MyStuff", "Stuff")" class="flex items-center gap-2 p-2 bg-gray-100 rounded">
            <svg class="h-4 w-4"><!-- Icono --></svg>
            My Stufff
            <span class="ml-auto text-xs bg-red-500 text-white px-2 py-0.5 rounded-full">4</span>
        </a>
        <!-- Uso de iterator para poder integrar cada módulo de forma dinámica -->
    </nav>
</aside>